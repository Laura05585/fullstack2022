cons tstr ='En un lugar de la mancha';
cons tstr2 ='En otro lugar';
const ultimoString1= str1.length-1; // guarda 23
const ultimoString1= str2.length-1; // guarda 12

if ((str1.chartAt (0) == str2.charAt(0)) && (str1.
    charAt (u1) == str2.charAt(u2))) {
    document.write("los dos string tienen la letra igual")
    } else {
    document.write("tienen la primer letra diferente")
    }